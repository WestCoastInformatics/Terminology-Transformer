// NDC controller
tsApp.controller('NdcCtrl', [ '$scope', '$location', 'securityService', 'utilService', 'appConfig',
  function($scope, $location, securityService, utilService, appConfig) {
    console.debug('configure NdcCtrl');

    utilService.clearError();

    // Set up scope

    // Define functions

    // Initialize
    

  } ]);